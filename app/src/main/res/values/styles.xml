<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <item name="colorPrimary">@color/color_201a11</item>
        <item name="colorPrimaryDark">@color/color_201a11</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->
        <item name="colorAccent">@color/color_201a11</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>



    <!-- dialog activity set -->
    <style name="ActivityDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>


    <!-- tab layout text size -->
    <style name="MyTabLayoutTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">15dp</item>
    </style>

    <!-- picker line color -->
    <style name="CustomPickerTheme" parent="AppTheme">
        <item name="colorControlNormal">#00ffffff</item>
    </style>


    <!-- edittext underline color -->
</resources>
